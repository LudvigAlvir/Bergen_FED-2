/**
 * function to sum two numbers
 * @param {number} a number 1
 * @param {number} b number 2
 * @returns {number} sum of a and b
 * @example
 * const x = sum(10, 15);
 */
function sum(a, b) {
	return a + b;
}
sum();
// Lag JSDocs for denne funksjonen
